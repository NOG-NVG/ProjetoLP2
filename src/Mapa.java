import java.util.ArrayList;

public class Mapa {
    int matriz;
    int nrPecas;
    ArrayList<Integer> IDs=new ArrayList<>();

}
/*
getmap(File file){
 File ficheiro = new File(folder, "mapa.txt");

        int nrLinha = 0;

        BufferedReader reader = null;

        try {
            reader = new BufferedReader(new FileReader(ficheiro));
        } catch (FileNotFoundException e) {
            return false;
        }
        String line = null;

        do {

            try {
                line = reader.readLine();
            } catch (IOException e) {
                throw new RuntimeException(e);
            }
            //TODO apos todas as verificacoes
            if (line != null) {
            if(nrLinha==0){
            matriz=line
            if(nrLinha==1)
            nrPecas=line
            else{
            String[] partes = line.split(":");
                for(int i=0;i<nrPecas;i++={
                IDs.get(i).add(i);
                }


            }
               nrLinha++;
            } while(linha!=null)
}
 */

